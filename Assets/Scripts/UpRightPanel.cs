using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UpRightPanel : MonoBehaviour
{

    void Start()
    {

        transform.position = new Vector3(1743, 730, 0);

    }

    // Update is called once per frame
    void Update()
    {
        transform.position = new Vector3(1743, 730, 0);
    }
}
